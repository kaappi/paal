;;; Generated by `make srfi-baseline`.  Do not hand-edit -- regenerate.
;;; See tests/srfi/README.md.  Skips live in paal-srfi-skip.sld.

(define-library (paal-srfi-expected)
  (export expected-verdicts)
  (begin
    ;; (file verdict) per vendored test file, in name order; a
    ;; fail line carries the reason as a trailing comment.
    (define expected-verdicts
      '(
        ("srfi-19-tests.scm" fail) ; needs (scheme time) native time objects (make-time, time?, time-second …) — a Phase 2 host-native binding
        ("srfi-254.scm" pass)
        ("srfi-258.scm" pass)
        ("srfi-27-unit.scm" pass)
        ("srfi0.scm" pass)
        ("srfi1-ext.scm" pass)
        ("srfi1.scm" pass)
        ("srfi101.scm" fail) ; car: not a pair (a test-runner record) — base-name capture across the static link
        ("srfi112.scm" pass)
        ("srfi113-audit.scm" pass)
        ("srfi113.scm" pass)
        ("srfi115.scm" pass)
        ("srfi116.scm" pass)
        ("srfi117.scm" pass)
        ("srfi118.scm" pass)
        ("srfi123.scm" pass)
        ("srfi125.scm" pass)
        ("srfi126.scm" pass)
        ("srfi127.scm" pass)
        ("srfi128.scm" pass)
        ("srfi129.scm" pass)
        ("srfi13-charset.scm" pass)
        ("srfi13-ext.scm" pass)
        ("srfi13-startend.scm" pass)
        ("srfi130.scm" pass)
        ("srfi132.scm" pass)
        ("srfi133-ext.scm" pass)
        ("srfi134.scm" pass)
        ("srfi135.scm" pass)
        ("srfi139.scm" fail) ; syntax-parameterize must adjust the live transformer through a macro boundary; paal binds let-syntax lexically
        ("srfi140.scm" pass)
        ("srfi141.scm" pass)
        ("srfi143.scm" pass)
        ("srfi144.scm" pass)
        ("srfi146-differential.scm" fail) ; paal-emitter: ir:define in expression position
        ("srfi146-reference.scm" pass)
        ("srfi146.scm" pass)
        ("srfi147.scm" fail) ; a bare-keyword transformer alias naming a macro that is not itself syntax-rules
        ("srfi148.scm" fail) ; syntax-rules: no matching pattern (em …) — needs the un-adapted (srfi 26), blocked on Phase 4
        ("srfi149.scm" fail) ; SRFI 149's relaxed ellipsis depths: a variable used outside its ellipsis template
        ("srfi151.scm" pass)
        ("srfi152-audit.scm" pass)
        ("srfi152.scm" pass)
        ("srfi153.scm" pass)
        ("srfi156.scm" fail) ; the `is` placeholder macro: every placeholder clause (12 of 20)
        ("srfi158-audit.scm" fail) ; 13 raises? assertions pin kaappi's #2060 restriction — resuming a coroutine generator inside a Scheme-level driver (SRFI 1 fold/filter/any/every, hash-table-walk) raises there.  paal's continuations survive it, so the assertions that demand a raise fail
        ("srfi158.scm" pass)
        ("srfi160.scm" pass)
        ("srfi161.scm" pass)
        ("srfi162.scm" pass)
        ("srfi164.scm" pass)
        ("srfi165.scm" pass)
        ("srfi166-audit.scm" fail) ; imports (srfi 59), which needs (kaappi sysinfo) — a Phase 2 host-native binding
        ("srfi166-sub.scm" pass)
        ("srfi166.scm" pass)
        ("srfi167.scm" pass)
        ("srfi168.scm" pass)
        ("srfi169.scm" pass)
        ("srfi17.scm" pass)
        ("srfi171.scm" pass)
        ("srfi173.scm" pass)
        ("srfi174.scm" pass)
        ("srfi175.scm" pass)
        ("srfi178-audit.scm" pass)
        ("srfi178.scm" pass)
        ("srfi180.scm" pass)
        ("srfi185.scm" pass)
        ("srfi188.scm" fail) ; a definition wrapped in `begin` at top level must not shadow — kaappi's own define-hoisting lenience (kaappi#2075), which paal does not copy
        ("srfi189-audit.scm" pass)
        ("srfi189.scm" pass)
        ("srfi190.scm" fail) ; the template's `yield` must capture the use site's references; paal renames template binders per R7RS hygiene
        ("srfi193.scm" pass)
        ("srfi194.scm" fail) ; the distribution generators (4 of 40)
        ("srfi195.scm" pass)
        ("srfi196.scm" pass)
        ("srfi197.scm" pass)
        ("srfi2.scm" pass)
        ("srfi201.scm" fail) ; paal-emitter: ir:define in expression position
        ("srfi202.scm" pass)
        ("srfi203.scm" pass)
        ("srfi207.scm" fail) ; the reader has no bytestring literal (#u8"…")
        ("srfi209.scm" pass)
        ("srfi210.scm" pass)
        ("srfi213.scm" fail) ; imports (srfi 211 explicit-renaming) — er-macros, recorded out of scope until after Phase 4
        ("srfi214.scm" fail) ; flexvector: 3 assertions
        ("srfi215.scm" pass)
        ("srfi217.scm" pass)
        ("srfi219.scm" fail) ; the last two cases wrap a curried define in `begin` in expression position — kaappi's define-hoisting lenience (kaappi#2073)
        ("srfi221.scm" pass)
        ("srfi222.scm" fail) ; a compound is a tagged vector here, so `(vector? c)` answers #t (1 of 24)
        ("srfi223.scm" pass)
        ("srfi224.scm" pass)
        ("srfi225-audit.scm" pass)
        ("srfi225.scm" pass)
        ("srfi227.scm" pass)
        ("srfi228.scm" pass)
        ("srfi23.scm" pass)
        ("srfi231-arrays.scm" pass)
        ("srfi231-assembly.scm" pass)
        ("srfi231-combinators.scm" pass)
        ("srfi231-intervals.scm" pass)
        ("srfi231-storage-classes.scm" pass)
        ("srfi231-views.scm" pass)
        ("srfi231.scm" pass)
        ("srfi232.scm" pass)
        ("srfi233.scm" pass)
        ("srfi234.scm" pass)
        ("srfi235.scm" pass)
        ("srfi236.scm" pass)
        ("srfi238.scm" pass)
        ("srfi239.scm" pass)
        ("srfi241.scm" pass)
        ("srfi242.scm" fail) ; label*: a static call must see the call site's own binding (2 of 14)
        ("srfi244.scm" fail) ; define-values with a rest formal: map over an improper list
        ("srfi247.scm" fail) ; the $ macros thread state variables into formals (4 of 12)
        ("srfi25.scm" pass)
        ("srfi250.scm" fail) ; a hash table keyed by a comparator record: not a callable (the record crosses as a tagged vector)
        ("srfi251.scm" pass)
        ("srfi253.scm" fail) ; a template-introduced constructor name bound only by a later expansion step — syntax-object provenance, a Phase 4 target
        ("srfi255.scm" fail) ; restarters resume through a continuation whose dispatch extent has ended
        ("srfi257-full.scm" fail) ; same as srfi257.scm
        ("srfi257-rx-full.scm" fail) ; same as srfi257.scm, reached through the rx pattern layer
        ("srfi257-rx.scm" fail) ; same as srfi257.scm, reached through the rx pattern layer
        ("srfi257.scm" fail) ; the free-identifier=? trick nested several levels deep: an inner let-syntax macro stops matching (the simple form of the same trick works)
        ("srfi259.scm" pass)
        ("srfi26.scm" pass)
        ("srfi260.scm" pass)
        ("srfi264-behavior.scm" pass)
        ("srfi267.scm" fail) ; the reader has no #"…" literal
        ("srfi27-audit.scm" pass)
        ("srfi27-state.scm" pass)
        ("srfi270.scm" pass)
        ("srfi30.scm" pass)
        ("srfi35-audit.scm" pass)
        ("srfi35.scm" pass)
        ("srfi36.scm" pass)
        ("srfi37.scm" pass)
        ("srfi38.scm" pass)
        ("srfi39.scm" pass)
        ("srfi4.scm" pass)
        ("srfi41.scm" fail) ; the 4 stream-of assertions — syntax-object provenance, a Phase 4 target
        ("srfi42.scm" fail) ; do-ec: no matching pattern on the threaded stop variable — syntax-object provenance, a Phase 4 target
        ("srfi43.scm" pass)
        ("srfi44.scm" pass)
        ("srfi45.scm" pass)
        ("srfi46.scm" fail) ; custom-ellipsis hygiene and two ellipses in one pattern (4 of 31)
        ("srfi48.scm" pass)
        ("srfi5.scm" pass)
        ("srfi51.scm" pass)
        ("srfi54.scm" pass)
        ("srfi59.scm" pass)
        ("srfi6.scm" pass)
        ("srfi60.scm" pass)
        ("srfi61.scm" pass)
        ("srfi62.scm" pass)
        ("srfi63.scm" pass)
        ("srfi64.scm" fail) ; order-dependent, and the shelf's own harness is the cause: passes standalone and in every small combination, fails in the full run once srfi4.scm's host numeric vectors have been allocated.  The assertion that goes is the SRFI 35 condition-type test-error.  Not reproducible in isolation — suspected allocation-dependent host state; see docs/TODO.md
        ("srfi66.scm" pass)
        ("srfi67.scm" pass)
        ("srfi69-ext.scm" pass)
        ("srfi69.scm" pass)
        ("srfi7.scm" pass)
        ("srfi70.scm" pass)
        ("srfi71.scm" pass)
        ("srfi78.scm" pass)
        ("srfi86.scm" pass)
        ("srfi87.scm" pass)
        ("srfi9.scm" fail) ; the 4 assertions that probe the tagged-vector record representation directly
        ("srfi90.scm" pass)
        ("srfi94.scm" pass)
        ("srfi95.scm" pass)
        ("srfi98.scm" pass)
        ))))
